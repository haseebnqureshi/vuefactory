{
	"config": "{{{config}}}",
	"deps": "var register = require('./register');\n{{{deps}}}",
	"configuring": "{{{configuring}}}",
	"stores": "{{{stores}}}",
	"routes": "routes = _.union(routes, register.routes);\n{{{routes}}}",
	"middlewares": "middlewares = _.union(middlewares, register.middlewares);\n{{{middlewares}}}"
}