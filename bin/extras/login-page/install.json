{
	"config": "{{{config}}}",
	"deps": "var loginPage = require('./login-page');\n{{{deps}}}",
	"configuring": "{{{configuring}}}",
	"stores": "{{{stores}}}",
	"routes": "routes = _.union(routes, loginPage.routes);\n{{{routes}}}",
	"middlewares": "middlewares = _.union(middlewares, loginPage.middlewares);\n{{{middlewares}}}"
}